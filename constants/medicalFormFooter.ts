/**
 * Height of the docked Save / Cancel bar on the medical add forms.
 * The Ask 3elagi AI FAB lifts itself by this much on those routes so it never
 * covers the buttons (padding 14 top + bottom, ~41 button, 1 border).
 */
export const MEDICAL_FORM_SAVE_BAR_HEIGHT = 70;
