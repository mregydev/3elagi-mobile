/**
 * Instant "call this doctor now" — ringing a doctor straight from chat or the
 * directory. Hidden for now; the booked video appointment flow is unaffected.
 * Flip to true to bring back the call button, the live/on-call badges and the
 * doctor's availability toggle.
 */
export const IMMEDIATE_VIDEO_CALL_ENABLED = false;
