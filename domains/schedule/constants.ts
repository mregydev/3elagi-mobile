export const DEFAULT_SLOT_MINUTES = 10;

export const DEFAULT_SCHEDULE_START = "09:00";

/** Default end — after 5 PM so evening availability is supported. */
export const DEFAULT_SCHEDULE_END = "21:00";
