/** Web / non-native stub — use app.native.ts on Android and iOS. */
export function isFirebaseReady(): boolean {
  return false;
}

export function getFirebaseProjectId(): string | null {
  return null;
}
